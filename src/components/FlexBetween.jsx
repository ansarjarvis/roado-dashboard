import { Box } from "@mui/system";
import { styled } from "@mui/system";

let FlexBewtween = styled(Box)({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
});

export default FlexBewtween;
